import java.util.*;
public class sumofNumbers
  {
    public static void main(String[] atg)
    {
      int sum=0, i=num;
      SC = new.Scanner(System.in);
      Sytem.out.println("enter any number:");
      while(i<=0)
        {
          frist_digit = frist_digit/10;
        }
      last_digit = last_digit%10;
      Sytem.out.println("\n given any number" + number+ "=" + frist_digit);
      System.out.println("\n given any number" + number+ "=" +last_digit);
    }
  }